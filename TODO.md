# TODO

- Set up the Vite TypeScript project entry points.
- Install and configure PixiJS.
- Create the Pixi application bootstrap.
- Create the game loop with update and render boundaries.
- Implement player movement through a movement system.
- Implement enemy spawning through a spawn system.
- Implement collision detection as an isolated system.
- Add a UI layer for score, health, and game state.
- Integrate the API client for external data.
- Optimize rendering, object allocation, and update cost.
- Prepare production build and deployment configuration.
